namespace Scheduling.Infrastructure.Domain.Appointments
{
    //public class AppointmentRepository : IAppointmentRepository
    //{
    //    private readonly AppointmentContext context;

    //    public AppointmentRepository(AppointmentContext context)
    //    {
    //        this.context = context;
    //    }
    //    public async Task AddAsync(Appointment.Domain.Appointments.Appointment appointment)
    //    {
    //        await context.AddAsync(appointment);
    //    }

    //    public async Task<Appointment.Domain.Appointments.Appointment> GetByIdAsync(Guid appointmentId)
    //    {
    //        return await context.Appointments.FirstOrDefaultAsync(x => x.Id == appointmentId);
    //    }
    //}
}
